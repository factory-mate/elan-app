import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_base/digital-modeling/products/craft/process/')({
  staticData: {
    title: '工序档案',
    permCode: 'process'
  }
})
